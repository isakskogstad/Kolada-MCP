# Kolada MCP Server Configuration

# ======================
# API Configuration
# ======================
KOLADA_API_BASE_URL=https://api.kolada.se/v3
KOLADA_RATE_LIMIT=5
KOLADA_CACHE_TTL=86400
KOLADA_TIMEOUT=30000

# ======================
# HTTP Server (for Lovable/web clients)
# ======================
# MCP_MODE can be "stdio" (default) or "http"
MCP_MODE=stdio

# Port for HTTP server (default: 3000)
PORT=3000

# Authentication token for HTTP mode
# Generate with: openssl rand -base64 32
MCP_AUTH_TOKEN=your-secure-token-here

# ======================
# Logging
# ======================
LOG_LEVEL=info
NODE_ENV=development
